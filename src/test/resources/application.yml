spring:
  datasource:
    url: jdbc:sqlite:file:./build/test-webhook.db
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

webhook:
  secret: test-secret
  hmac:
    enabled: false